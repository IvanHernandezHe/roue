# Copy to `.env.intranet` and update values to match your intranet setup.
# Any variable left empty will fall back to the defaults in docker-compose.roue.yml.

# Public URL that browsers on your intranet will use to reach the Angular app.
PUBLIC_WEB_ORIGIN=http://roue.my.lan:4200

# Optional Stripe keys for intranet testing.
STRIPE__APIKEY=
STRIPE__PUBLISHABLEKEY=
